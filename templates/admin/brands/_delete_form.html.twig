<form method="post" action="{{ path('admin_brands_delete', {'id': brand.id}) }}" id="delete-{{ brand.id }}">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ brand.id) }}">
</form>